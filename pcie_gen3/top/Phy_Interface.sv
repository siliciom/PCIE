interface phy_tx_interface();
  
  logic TX;  // Transmitting serial data to EP
  logic RX;  // Receiving serial data from RC

endinterface

interface phy_rx_interface();
  
  logic TX;  // Transmitting serial data to EP
  logic RX;  // Receiving serial data from RC

endinterface
